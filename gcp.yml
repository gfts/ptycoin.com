apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: brewers 
  labels:
    name: brewers
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: brewers
    spec:
      containers:
      - name: brewers
        image: gcr.io/gfts-internal/ptycoin-web:1.0.0-pr.0
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: brewers
  labels:
    name: brewers
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
  selector:
    name: brewers

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: brewers
spec:
  backend:
    serviceName: brewers
    servicePort: 80
